@model IList<Product>
@using FashionWeb.Utilities;

    <div class="container-fluid pt-5 pb-3">
        <h2 class="section-title position-relative text-uppercase mx-xl-5 mb-4"><span class="bg-secondary pr-3">Sản phẩm nổi bật</span></h2>
        <div class="row px-xl-5">
            @foreach(var prod in Model.Where(m => m.IsActive == true))
            {
            <partial name="_Product" model="prod" />
            }
        </div>
    </div>